#include "holberton"
#include <stdio.h>

/**
 * main - Writes its name
 * @argc: Counts of argument
 * @argv: argument vector
 * Return: 0
 */

int main(int argc, char *argv[])
{
(void)argc;
printf("%s", argv);
return (0);
}
