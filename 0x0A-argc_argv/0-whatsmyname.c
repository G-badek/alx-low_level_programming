#include "holberton.h"
#include <stdio.h>

/**
 * main - Writes its name
 * @argc: Counts of argument
 * @argv: argument vector
 * Return: 0
 */

int main(int argc, char *argv[])
{
argc = argc;
printf("%s\n", *argv);
return (0);
}
