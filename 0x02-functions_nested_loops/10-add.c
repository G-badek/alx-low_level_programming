#include "holberton.h"

/**
 * add - Adds two integers and returns the result
 * @a: An integer
 * @b: Also an integer
 * Return: The sum of integers (Success)
 */
int add(int a, int b)
{
int sum = a + b;
return (sum);
}
